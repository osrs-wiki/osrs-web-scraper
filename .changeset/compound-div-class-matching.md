---
"osrs-web-scraper": patch
---

Match `div` class parsers (e.g. `image-caption`) by individual class token so compound class names like `image-caption image-caption--empty` still resolve correctly.
