---
"osrs-web-scraper": patch
---

Ignore the `osrsTabNavigation` div and top-level `link`/`button` tags (e.g. stylesheet links, back-to-top buttons) so they no longer render as "Unsupported tag" comments.
